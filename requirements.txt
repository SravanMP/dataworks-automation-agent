# To ensure app dependencies are ported from your virtual environment/host machine into your container, run 'pip freeze > requirements.txt' in the terminal to overwrite this file
fastapi[all]==0.109.0
uvicorn[standard]==0.27.0
gunicorn==21.2.0
requests==2.31.0  # Added requests library to avoid ModuleNotFoundError

# After updating the requirements.txt, rebuild and push your Docker image:
# docker build -t sravanmp/dataworks-automation-agent .
# docker push sravanmp/dataworks-automation-agent

# Finally, run your container:
# docker run -e AIPROXY_TOKEN=your_token -p 8000:8000 sravanmp/dataworks-automation-agent
